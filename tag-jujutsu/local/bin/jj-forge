#!/usr/bin/env bash

set -euo pipefail

jj git remote list | while read -r name url; do
  template='git_web_url("'"$name"'")'
  url="$(jj log --no-graph -n 1 -T "$template")"
  printf "%s\n" "$url"
  open "$url"
done
