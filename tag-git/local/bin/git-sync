#!/bin/sh

set -eu

git fetch origin --prune
git checkout master
git pull origin master --tags
git branch --no-color --merged | grep -v '\*\|master$' |
  xargs -n 1 git branch -d 2>&1 | grep -v 'fatal: branch name required'
