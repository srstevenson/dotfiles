# ~/.config/git/config

[commit]
    gpgSign = true

[gpg]
    program = gpg2

[log]
    decorate = true

[pretty]
    gls = %C(auto)%h %s %C(cyan)(%ar, %an)%C(auto)%d%n

[push]
    default = simple

[transfer]
    fsckObjects = true

[user]
    email = scott@stevenson.io
    name = Scott Stevenson
    signingKey = 0xEF25C8864C0618DC
