# ~/.config/git/config

[init]
    defaultBranch = main

[pretty]
    dense = %C(auto)%h %s %C(cyan)<%an> %C(green)(%ar)%C(auto)%d%n

[pull]
    ff = only

[user]
    email = scott@stevenson.io
    name = Scott Stevenson
